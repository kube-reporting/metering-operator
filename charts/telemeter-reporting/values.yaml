spec:
  defaultReportDataSources:
    cpus-by-node-role:
      spec:
        promsum:
          query: "cpus-by-node-role"
    nodes-by-instance-type:
      spec:
        promsum:
          query: "nodes-by-instance-type"

  defaultStorage:
    create: true
    name: "hive-hdfs"
    isDefault: true
    type: "hive"
    hive:
      tableProperties:
        location: "hdfs://hdfs-namenode-proxy:9820/operator_metering/storage/"

