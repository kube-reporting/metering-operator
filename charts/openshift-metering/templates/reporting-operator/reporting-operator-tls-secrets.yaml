{{- $operatorValues :=  index .Values "reporting-operator" -}}
{{- if $operatorValues.spec.con***REMOVED***g.tls.createSecret -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $operatorValues.spec.con***REMOVED***g.tls.secretName }}
  labels:
    app: reporting-operator
type: kubernetes.io/tls
data:
  tls.crt: {{ $operatorValues.spec.con***REMOVED***g.tls.certi***REMOVED***cateData | b64enc | quote }}
  tls.key: {{ $operatorValues.spec.con***REMOVED***g.tls.privateKeyData | b64enc | quote }}
{{- end -}}
