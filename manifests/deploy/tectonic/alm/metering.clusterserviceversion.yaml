#! validate-crd: ../../deploy/chart/kube-1.8/templates/03-clusterserviceversion.crd.yaml
#! parse-kind: ClusterServiceVersion
apiVersion: app.coreos.com/v1alpha1
kind: ClusterServiceVersion-v1
metadata:
  name: metering-helm-operator.v0.6.0-latest
  annotations:
    tectonic-visibility: tectonic-feature
  labels:
    alm-catalog: tectonic-feature
spec:
  displayName: Metering
  description: Metering can generate reports based on historical usage data from a cluster, providing accountability for how resources have been used.
  keywords: [metering metrics reporting coreos]
  version: 0.6.0-latest
  maturity: alpha
  maintainers:
    - email: support@coreos.com
      name: CoreOS, Inc
  provider:
    name: CoreOS, Inc
  labels:
    alm-owner-metering: metering-helm-operator
    alm-status-descriptors: metering-helm-operator.v0.6.0-latest
  selector:
    matchLabels:
      alm-owner-metering: metering-helm-operator
  install:
    strategy: deployment
    spec:
      permissions:
        null
      deployments:
        null
  customresourcede***REMOVED***nitions:
    null
