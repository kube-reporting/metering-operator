{{- if .Values.hadoop.spec.con***REMOVED***g.s3Compatible.ca.createSecret -}}
apiVersion: v1
kind: Secret
metadata:
  name: hadoop-s3-compatible-ca
  labels:
    app: hadoop
type: Opaque
data:
  ca-bundle.crt: {{ .Values.hadoop.spec.con***REMOVED***g.s3Compatible.ca.content | b64enc | quote }}
{{- end -}}
