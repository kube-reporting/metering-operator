apiVersion: v1
kind: Service
metadata:
  labels:
    app: presto
  name: presto
  namespace: tectonic-chargeback
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    presto: coordinator
