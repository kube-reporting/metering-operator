{{- if .Values.spec.con***REMOVED***g.tls.createSecret -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.spec.con***REMOVED***g.tls.secretName }}
  labels:
    app: metering
{{- block "extraMetadata" . }}
{{- end }}
type: kubernetes.io/tls
data:
  tls.crt: {{ .Values.spec.con***REMOVED***g.tls.certi***REMOVED***cateData | b64enc | quote }}
  tls.key: {{ .Values.spec.con***REMOVED***g.tls.privateKeyData | b64enc | quote }}
{{- end -}}
