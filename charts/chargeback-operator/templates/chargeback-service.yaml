kind: Service
apiVersion: v1
metadata:
  name: chargeback
  labels:
    app: chargeback
{{- block "extraMetadata" . }}
{{- end }}
spec:
  selector:
    app: chargeback
  ports:
  - protocol: TCP
    port: 8080
    targetPort: "http"
