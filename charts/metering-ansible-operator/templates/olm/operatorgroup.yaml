apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ .Values.olm.operatorGroupName }}
  namespace: {{ .Values.olm.catalogSourceCon***REMOVED***gTargetNamespace }}
spec:
  targetNamespaces:
{{ toYaml .Values.olm.operatorGroupTargetNamespaces | indent 2 }}
