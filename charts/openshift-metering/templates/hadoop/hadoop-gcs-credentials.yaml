{{- if .Values.hadoop.spec.con***REMOVED***g.gcs.createSecret -}}
apiVersion: v1
kind: Secret
metadata:
  name: hadoop-gcs-credentials
data:
{{- if .Values.hadoop.spec.con***REMOVED***g.gcs.serviceAccountKeyJSON }}
  gcs-service-account.json: {{ .Values.hadoop.spec.con***REMOVED***g.gcs.serviceAccountKeyJSON | toJson | b64enc | quote }}
{{- end }}
{{- end -}}
