{{- if and .Values.presto.spec.con***REMOVED***g.sharedVolume.enabled .Values.presto.spec.con***REMOVED***g.sharedVolume.createPVC }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.presto.spec.con***REMOVED***g.sharedVolume.claimName }}
spec:
  accessModes:
  - ReadWriteMany
{{- if .Values.presto.spec.con***REMOVED***g.sharedVolume.storageClass }}
  storageClassName: {{ .Values.presto.spec.con***REMOVED***g.sharedVolume.storageClass }}
{{- end }}
  resources:
    requests:
      storage: {{ .Values.presto.spec.con***REMOVED***g.sharedVolume.size }}
{{- end }}
