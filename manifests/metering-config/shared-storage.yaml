apiVersion: metering.openshift.io/v1alpha1
kind: MeteringCon***REMOVED***g
metadata:
  name: "operator-metering"
spec:
  storage:
    hive:
      type: "sharedPVC"
      sharedPVC:
        claimName: "metering-nfs"
        # uncomment the lines below to provision a new PVC using the speci***REMOVED***ed
        # storageClass.
        # createPVC: true
        # storageClass: "my-nfs-storage-class"
        # size: 5Gi
