{{- if and .Values.hive.spec.con***REMOVED***g.sharedVolume.enabled .Values.hive.spec.con***REMOVED***g.sharedVolume.createPVC }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.hive.spec.con***REMOVED***g.sharedVolume.claimName }}
spec:
  accessModes:
  - ReadWriteMany
{{- if .Values.hive.spec.con***REMOVED***g.sharedVolume.storageClass }}
  storageClassName: {{ .Values.hive.spec.con***REMOVED***g.sharedVolume.storageClass }}
{{- end }}
  resources:
    requests:
      storage: {{ .Values.hive.spec.con***REMOVED***g.sharedVolume.size }}
{{- end }}
