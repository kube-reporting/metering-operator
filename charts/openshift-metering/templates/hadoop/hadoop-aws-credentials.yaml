{{- if .Values.hadoop.spec.con***REMOVED***g.aws.createSecret -}}
apiVersion: v1
kind: Secret
metadata:
  name: hadoop-aws-credentials
data:
{{- if .Values.hadoop.spec.con***REMOVED***g.aws.accessKeyID }}
  aws-access-key-id: {{ .Values.hadoop.spec.con***REMOVED***g.aws.accessKeyID | b64enc | quote}}
{{- end}}
{{- if .Values.hadoop.spec.con***REMOVED***g.aws.secretAccessKey }}
  aws-secret-access-key: {{ .Values.hadoop.spec.con***REMOVED***g.aws.secretAccessKey | b64enc | quote}}
{{- end}}
{{- end -}}
