apiVersion: chargeback.coreos.com/v1alpha1
kind: Metering
metadata:
  name: "operator-metering"
spec:
  reporting-operator:
    spec:
      image:
        tag: latest

  presto:
    spec:
      presto:
        image:
          tag: latest
      hive:
        image:
          tag: latest

  hdfs:
    spec:
      image:
        tag: latest
