apiVersion: metering.openshift.io/v1alpha1
kind: Metering
metadata:
  name: "operator-metering"
spec:
  # Uncomment the section below if you wish to adjust the StorageClass

  # presto:
  #   spec:
  #     hive:
  #       metastore:
  #         storage:
  #           # Default is null, which means using the default storage class if it exists. If you wish to use a different storage class, specify it here
  #           class: null
  #           size: "5Gi"


  # Uncomment the section below if you wish to adjust the StorageClass

  # hdfs:
  #   spec:
  #     datanode:
  #       storage:
  #         # Default is null, which means using the default storage class if it exists. If you wish to use a different storage class, specify it here
  #         class: null
  #         size: "5Gi"
  #     namenode:
  #       storage:
  #         size: "5Gi"
  #         class: null
