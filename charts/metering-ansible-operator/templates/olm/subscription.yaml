apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ .Values.olm.subscriptionName }}
spec:
  channel: {{ .Values.olm.subscriptionChannel }}
  name: {{ .Values.olm.packageName }}
  source: {{ .Values.olm.catalogSourceCon***REMOVED***gName  }}
  sourceNamespace: {{ .Values.olm.catalogSourceCon***REMOVED***gTargetNamespace }}
