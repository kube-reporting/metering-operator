{{- if and .Values.presto.spec.con***REMOVED***g.sharedVolume.enabled .Values.presto.spec.con***REMOVED***g.sharedVolume.createPVC }}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: hive-warehouse-data
{{- block "extraMetadata" . }}
{{- end }}
spec:
  accessModes:
  - ReadWriteMany
{{- if .Values.presto.spec.con***REMOVED***g.sharedVolume.storage.persistentVolumeClaimStorageClass }}
  storageClassName: {{ .Values.presto.spec.con***REMOVED***g.sharedVolume.storage.persistentVolumeClaimStorageClass }}
{{- end }}
  resources:
    requests:
      storage: {{ .Values.presto.spec.con***REMOVED***g.sharedVolume.storage.persistentVolumeClaimSize }}
{{- end }}
