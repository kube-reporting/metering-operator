apiVersion: app.coreos.com/v1alpha1
kind: Subscription-v1
metadata:
  name: {{ .Values.name }}
spec:
  channel: {{ .Values.subscriptionChannel }}
  name: {{ .Values.packageName }}
  source: tectonic-components
