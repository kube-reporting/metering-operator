{{- $operatorValues :=  index .Values "reporting-operator" -}}
{{- if $operatorValues.spec.con***REMOVED***g.presto.tls.createSecret -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $operatorValues.spec.con***REMOVED***g.presto.tls.secretName }}
  labels:
    app: reporting-operator
type: Opaque
data:
  ca.crt: {{ $operatorValues.spec.con***REMOVED***g.presto.tls.caCerti***REMOVED***cate | b64enc | quote }}
{{- end -}}
