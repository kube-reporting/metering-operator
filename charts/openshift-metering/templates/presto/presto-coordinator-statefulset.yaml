apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: presto-coordinator
  labels:
    app: presto
    presto: coordinator
spec:
  replicas: 1
  serviceName: presto-nodes
  selector:
    matchLabels:
      app: presto
      presto: coordinator
{{- if .Values.presto.spec.labels }}
{{ toYaml .Values.presto.spec.labels | indent 6 }}
{{- end }}
  template:
    metadata:
      labels:
        app: presto
        presto: coordinator
{{- if .Values.presto.spec.labels }}
{{ toYaml .Values.presto.spec.labels | indent 8 }}
{{- end }}
      annotations:
        presto-coordinator-con***REMOVED***g-hash: {{ include (print $.Template.BasePath "/presto/presto-coordinator-con***REMOVED***g.yaml") . | sha256sum }}
        presto-common-con***REMOVED***g-hash: {{ include (print $.Template.BasePath "/presto/presto-common-con***REMOVED***g.yaml") . | sha256sum }}
        presto-catalog-con***REMOVED***g-hash: {{ include (print $.Template.BasePath "/presto/presto-catalog-con***REMOVED***g-secret.yaml") . | sha256sum }}
        presto-jmx-con***REMOVED***g-hash: {{ include (print $.Template.BasePath "/presto/presto-jmx-con***REMOVED***g.yaml") . | sha256sum }}
{{- if .Values.presto.spec.con***REMOVED***g.aws.createSecret }}
        presto-aws-credentials-hash: {{ include (print $.Template.BasePath "/presto/presto-aws-credentials-secret.yaml") . | sha256sum }}
{{- end }}
{{- if .Values.presto.spec.con***REMOVED***g.azure.createSecret }}
        presto-azure-credentials-secret-hash: {{ include (print $.Template.BasePath "/presto/presto-azure-credentials-secret.yaml") . | sha256sum }}
{{- end }}
{{- if .Values.presto.spec.con***REMOVED***g.gcs.createSecret }}
        presto-gcs-credentials-secret-hash: {{ include (print $.Template.BasePath "/presto/presto-gcs-credentials-secret.yaml") . | sha256sum }}
{{- end }}
{{- if .Values.presto.spec.con***REMOVED***g.s3Compatible.createSecret }}
        presto-s3-compatible-credentials-secret-hash: {{ include (print $.Template.BasePath "/presto/presto-s3-compatible-credentials-secret.yaml") . | sha256sum }}
{{- end }}
{{- if .Values.presto.spec.con***REMOVED***g.s3Compatible.ca.createSecret }}
        presto-s3-compatible-ca-secret-hash: {{ include (print $.Template.BasePath "/presto/presto-s3-compatible-ca-secret.yaml") . | sha256sum }}
{{- end }}
{{- if and .Values.presto.spec.con***REMOVED***g.tls.enabled .Values.presto.spec.con***REMOVED***g.tls.createSecret }}
        presto-server-tls-hash: {{ include (print $.Template.BasePath "/presto/presto-tls-secrets.yaml") . | sha256sum }}
{{- if and .Values.presto.spec.con***REMOVED***g.auth.enabled .Values.presto.spec.con***REMOVED***g.auth.createSecret }}
        presto-client-tls-hash: {{ include (print $.Template.BasePath "/presto/presto-auth-secrets.yaml") . | sha256sum }}
{{- end }}
{{- end }}
{{- if and .Values.presto.spec.con***REMOVED***g.connectors.hive.tls.enabled .Values.presto.spec.con***REMOVED***g.connectors.hive.tls.createSecret }}
        presto-hive-metastore-tls-hash: {{ include (print $.Template.BasePath "/presto/presto-hive-metastore-tls-secrets.yaml") . | sha256sum }}
{{- end }}
{{- if .Values.presto.spec.annotations }}
{{ toYaml .Values.presto.spec.annotations | indent 8 }}
{{- end }}
    spec:
{{- if .Values.useIPV6Networking }}
      hostAliases:
      - ip: "::1"
        hostnames:
          - "presto.local"
          - "presto"
{{- end }}
{{- if .Values.presto.spec.securityContext }}
      securityContext:
{{ toYaml .Values.presto.spec.securityContext | indent 8 }}
{{- end }}
{{- if .Values.presto.spec.coordinator.af***REMOVED***nity }}
      af***REMOVED***nity:
{{ toYaml .Values.presto.spec.coordinator.af***REMOVED***nity | indent 8 }}
{{- end }}
{{- if .Values.presto.spec.coordinator.nodeSelector }}
      nodeSelector:
{{ toYaml .Values.presto.spec.coordinator.nodeSelector | indent 8 }}
{{- end }}
{{- if .Values.presto.spec.coordinator.tolerations }}
      tolerations:
{{ toYaml .Values.presto.spec.coordinator.tolerations | indent 8 }}
{{- end }}
      initContainers:
      - name: copy-presto-con***REMOVED***g
        image: "{{ include "presto-image" . }}"
        imagePullPolicy: {{ .Values.presto.spec.image.pullPolicy }}
        command: ['/presto-common/initialize_presto.sh']
        # Copy the mounted con***REMOVED***guration data into the presto-etc emptyDir volume so we can write to the con***REMOVED***g ***REMOVED***les
        env:
{{- if .Values.presto.spec.con***REMOVED***g.connectors.prometheus.enabled }}
        - name: PRESTO_PROMETHEUS_CONNECTOR_BEARER_TOKEN_FILE
          valueFrom:
            con***REMOVED***gMapKeyRef:
              name: presto-coordinator-con***REMOVED***g
              key: prometheus-connector-bearer-token-***REMOVED***le
{{- end }}
{{- include "presto-common-env" . | indent 8 }}
        volumeMounts:
        - name: presto-etc
          mountPath: /opt/presto/presto-server/etc
        - name: presto-coordinator-con***REMOVED***g
          mountPath: /presto-etc
        - name: presto-catalog-con***REMOVED***g
          mountPath: /presto-etc/catalog
        - name: presto-data
          mountPath: /var/presto/data
        - name: presto-common-con***REMOVED***g
          mountPath: /presto-common
        resources:
          limits:
            cpu: 100m
            memory: 100Mi
          requests:
            cpu: 100m
            memory: 100Mi
      containers:
      - name: presto
        image: "{{ include "presto-image" . }}"
        imagePullPolicy: {{ .Values.presto.spec.image.pullPolicy }}
        command: ['/presto-common/entrypoint.sh']
        args: ['/opt/presto/presto-server/bin/launcher', 'run']
        env:
{{- include "presto-common-env" . | indent 8 }}
        ports:
        - name: api
          containerPort: 8080
          protocol: TCP
        - name: metrics
          containerPort: 8082
          protocol: TCP
        volumeMounts:
        - name: presto-etc
          mountPath: /opt/presto/presto-server/etc
        - name: presto-common-con***REMOVED***g
          mountPath: /presto-common
        - name: presto-jmx-con***REMOVED***g
          mountPath: /opt/jmx_exporter/con***REMOVED***g
        - name: presto-data
          mountPath: /var/presto/data
        - name: presto-logs
          mountPath: /var/presto/logs
{{- if .Values.hive.spec.con***REMOVED***g.sharedVolume.enabled }}
        - name: hive-warehouse-data
          mountPath: /var/presto-hive/
{{- end }}
        resources:
{{ toYaml .Values.presto.spec.coordinator.resources | indent 10 }}
      volumes:
      - name: presto-coordinator-con***REMOVED***g
        con***REMOVED***gMap:
          name: presto-coordinator-con***REMOVED***g
      - name: presto-common-con***REMOVED***g
        con***REMOVED***gMap:
          name: presto-common-con***REMOVED***g
          defaultMode: 0775
      - name: presto-catalog-con***REMOVED***g
        secret:
          secretName: presto-catalog-con***REMOVED***g
      - name: presto-jmx-con***REMOVED***g
        con***REMOVED***gMap:
          name: presto-jmx-con***REMOVED***g
      - name: presto-etc
        emptyDir: {}
      - name: presto-data
        emptyDir: {}
      - name: presto-logs
        emptyDir: {}
{{- if or .Values.presto.spec.con***REMOVED***g.gcs.secretName .Values.presto.spec.con***REMOVED***g.gcs.createSecret }}
      - name: gcs-json
        secret:
          secretName: {{ .Values.presto.spec.con***REMOVED***g.gcs.secretName | default "presto-gcs-credentials" }}
{{- end }}
{{- if or .Values.presto.spec.con***REMOVED***g.s3Compatible.ca.secretName .Values.presto.spec.con***REMOVED***g.s3Compatible.ca.createSecret }}
      - name: s3-compatible-ca
        secret:
          secretName: {{ .Values.presto.spec.con***REMOVED***g.s3Compatible.ca.secretName | default "presto-s3-compatible-ca" }}
{{- end }}
{{- if .Values.presto.spec.con***REMOVED***g.tls.enabled }}
      - name: presto-tls
        emptyDir: {}
      - name: presto-server-pem
        secret:
          secretName: {{ .Values.presto.spec.con***REMOVED***g.tls.secretName }}
{{- if .Values.presto.spec.con***REMOVED***g.auth.enabled }}
      - name: presto-client-pem
        secret:
          secretName: {{ .Values.presto.spec.con***REMOVED***g.auth.secretName }}
{{- end }}
{{- end }}
{{- if .Values.hive.spec.con***REMOVED***g.sharedVolume.enabled }}
      - name: hive-warehouse-data
        persistentVolumeClaim:
          claimName: {{ .Values.hive.spec.con***REMOVED***g.sharedVolume.claimName }}
{{- end }}
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      serviceAccount: presto
{{- if .Values.presto.spec.imagePullSecrets }}
      imagePullSecrets:
{{ toYaml .Values.presto.spec.imagePullSecrets | indent 8 }}
{{- end }}
      terminationGracePeriodSeconds: {{ .Values.presto.spec.coordinator.terminationGracePeriodSeconds }}
