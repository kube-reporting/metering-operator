apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ .Values.name }}
spec:
  channel: {{ .Values.subscriptionChannel }}
  name: {{ .Values.packageName }}
  source: rh-operators
