spec:
  defaultReportDataSources:
    vcpus-by-role:
      spec:
        promsum:
          query: "vcpus-by-role"
    nodes-by-type:
      spec:
        promsum:
          query: "nodes-by-type"

  defaultStorage:
    create: true
    name: "hive-hdfs"
    isDefault: true
    type: "hive"
    hive:
      tableProperties:
        location: "hdfs://hdfs-namenode-proxy:9820/operator_metering/storage/"

