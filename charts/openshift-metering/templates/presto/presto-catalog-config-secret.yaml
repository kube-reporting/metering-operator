apiVersion: v1
kind: Secret
metadata:
  name: presto-catalog-con***REMOVED***g
  labels:
    app: presto
type: Opaque
data:
  hive.properties: "{{ include "presto-hive-catalog-properties" . | b64enc }}"
  jmx.properties: "{{ include "presto-jmx-catalog-properties" . | b64enc }}"
{{- range $_, $connector := .Values.presto.spec.presto.con***REMOVED***g.connectors.extraConnectorFiles }}
  {{ $connector.name }}: "{{ $connector.content | b64enc }}"
{{- end }}
