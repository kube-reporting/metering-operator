apiVersion: chargeback.coreos.com/v1alpha1
kind: ReportDataStore
metadata:
  name: "aws-billing"
  labels:
    tectonic-chargeback: "true"
spec:
  awsBilling:
    source:
      bucket: "REPLACEME" # Provide the bucket name containing your AWS Detailed billing reports

